{
  "name": "jan-trejbal-cv",
  "type": "module",
  "version": "1.0.0",
  "description": "Jan Trejbal's CV website built with Astro",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:pdf": "node scripts/generatepdf.js",
    "build:pdf": "bun run build && bun run preview & sleep 3 && bun run generate:pdf http://localhost:4321 jan-trejbal.pdf && pkill -f 'astro preview' || true"
  },
  "dependencies": {
    "astro": "^5.17.2"
  },
  "devDependencies": {
    "puppeteer": "^24.37.3"
  }
}
